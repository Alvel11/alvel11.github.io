<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /
    
    # No reescribir si el archivo existe
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    
    # Reescribir rutas a index.php
    RewriteRule ^(.*)$ index.php [QSA,L]
</IfModule>
